bin/
obj/
*.user
.vs/
*.suo
packages/
.DS_Store
**/.DS_Store
# Build outputs go to bin/ which is already ignored. The published exe in
# src/SineTest/sinetest.exe is intentionally tracked so it ships with the
# repo for easy testing on Windows.
*.pdb
